<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.company.store.infrastructure.mybatis.repository.book.BookMapper">
    <select id="findById" resultType="com.company.store.domain.book.Book" parameterType="long">
        SELECT * FROM book WHERE book_id = #{id}
    </select>
</mapper>